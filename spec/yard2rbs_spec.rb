# frozen_string_literal: true

RSpec.describe Yard2rbs do
  it "has a version number" do
    expect(Yard2rbs::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
